#!/bin/sh
# Pre-commit hook — runs ESLint on staged frontend files.
# Install: npx husky  (from the repo root after `npm install`)

set -e

echo "🔍  Running ESLint on frontend…"
cd frontend && npm run lint
echo "✅  Lint passed."
